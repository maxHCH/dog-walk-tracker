import Foundation

// 範本檔。複製成 DogWalk/Secrets.swift 並填入實際值後即可建置。
// （放在同步資料夾外，所以本檔不會被編譯。）

enum Secrets {
    static let supabaseURL = URL(string: "https://xxxx.supabase.co")!
    static let supabaseAnonKey = "your_supabase_anon_key"
}
